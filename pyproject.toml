[project]
name = "first-tekuteku"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "papparapa888", email = "k.taka2111@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "mujoco>=3.13.0",
    "numpy>=2.5.3",
    "scipy>=1.18.1",
]

[project.scripts]
first-tekuteku = "first_tekuteku:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
